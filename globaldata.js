var count = 0;    // how many times a group take a quiz
var id = 1;       // track id of a questions / Last questions insert total points into table 
var options = ""; // options 
var total = 0;    // total points
 
module.exports.count = count;
module.exports.id = id;
module.exports.options = options;
module.exports.total = total;
